// AI routes are now exposed via DocumentNodesController (/document-nodes/:id/ai/:action).
// This file is kept as a stub — the AiModule still exports AiService for use there.
import { Controller } from '@nestjs/common';

@Controller()
export class AiController {}
